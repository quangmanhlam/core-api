export const COMMANDS = {
    CREATE: 'create',
    ROLLBACK: 'rollback',
}

export const COMMANDS_LIST = [
    COMMANDS.CREATE,
    COMMANDS.ROLLBACK,
]

export const COLLECTIONS = {
    ALL: 'all',
    SETTINGS: 'settings',
    USERS: 'users',
    ACTIONS: 'actions',
    PERMISSIONS: 'permissions',
    ROLES: 'roles',
}

export const COLLECTION_NAMES = [
    COLLECTIONS.SETTINGS,
    COLLECTIONS.USERS,
    COLLECTIONS.ACTIONS,
    COLLECTIONS.PERMISSIONS,
    COLLECTIONS.ROLES,
]

export const MIGRATION_API_KEY = 'MIGRATION_API_KEY';
